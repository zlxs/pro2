访问路径中找不到对应的namespace则会返回给tomcat处理

struts2中在jsp中写的相对路径是是相对action的路径而不是jsp路径，
就是说 使用相对路径是相对action的映射路径（当前访问者请求的路径url），而不是web的映射路径
所以尽量不要使用相对路径


虽然可以用redirect方式解决，但redirect方式并非必要。

解决办法非常简单，统一使用绝对路径（在jsp中用request.getContextPath方式
来拿到webapp的路径，或者使用myeclipse经常用的，指定basePath

