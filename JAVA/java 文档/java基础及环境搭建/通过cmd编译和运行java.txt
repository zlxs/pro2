1.编译java类文件生成字节码
   javac 类名.java ,如果不在类的文件夹下，需要添加完整路径 如： javac F:/src/类名.java
   也可以通过-d来指定字节码生成在哪个路径下 javac -d F:/bin 类名.java 把字节码放在F:/bin下

2.运行字节码文件   java原文件和字节码别放在一个目录下 
   java 全限定类名(包名+类名 不需要后缀) 需要进入类字节码的文件夹目录下执行指令
   也可以通过 -cp来指定去哪个文件夹下寻找类的字节码 如 java -cp F:/bin 全限定类名(包名+类名) 
